# 이 파일을 keystore.properties로 복사하고 실제 값 입력
# keystore.properties는 절대 git에 올리지 말 것

storeFile=../keystore/bidet-release.jks
storePassword=your_store_password
keyAlias=bidet
keyPassword=your_key_password
